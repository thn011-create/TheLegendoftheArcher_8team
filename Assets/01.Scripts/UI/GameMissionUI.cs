using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameMissionUI : BaseUI
{
    // Start is called before the first frame update
    void Start()
    {
       // uiState = UIState.GameMission;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
